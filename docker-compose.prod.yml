services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: devops
      POSTGRES_PASSWORD: secret123
      POSTGRES_DB: appdb
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U devops -d appdb"]
      interval: 5s
      timeout: 5s
      retries: 5

  web:
    image: ${DOCKER_USERNAME}/fastapi-app:latest
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://devops:secret123@db:5432/appdb
    depends_on:
      db:
        condition: service_healthy

volumes:
  postgres_data:
